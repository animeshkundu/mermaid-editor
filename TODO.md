# TODO: Slice #1 – Flowchart Builder

- [ ] Research Mermaid flow syntax + React Flow APIs (handles, edges, custom nodes)
- [ ] Scaffold Visual Builder mode with React Flow canvas bound to Monaco text
- [ ] Render static flowchart from sample state to confirm serializer baseline
- [ ] Add node via palette button and regenerate Mermaid text
- [ ] Edit node label (inline or inspector) with live code sync
- [ ] Connect nodes via drag handles with validation and serialization
- [ ] Implement delete/backspace + undo/redo for add/edit/connect/delete
- [ ] Add unit tests for flowchart serializer/parser and sync loop
- [ ] Add Playwright e2e for add → connect → rename → delete → undo/redo
- [ ] Review parity vs mermaidchart.com/play and record next gaps
